# No secrets are required to run this project.
# This file exists only so the app can load an .env via python-dotenv without
# failing. The single placeholder below is optional and unused by default.

# Directory used for the on-disk cold tier (optional; defaults to ./data/cold_tier).
CACHE_DIR=./data/cold_tier
